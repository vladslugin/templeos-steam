/* Task sh_print - reference solution.

One line, and no function call in it. The shell is the compiler, so a string
used as a statement is compiled into a call that prints it - Compiler/PrsExp.HC
turns a bare string statement into exactly that.

Terry's own version of this file is /Doc/HelloWorld.DD, and it is no longer.
*/

"Hello, World\n";
