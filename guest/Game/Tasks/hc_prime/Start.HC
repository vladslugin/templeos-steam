/* Task hc_prime - Prime

  IsPrime(n) is TRUE when n has exactly two divisors, 1 and n itself.

  1 is not prime. 2 is.

The checker calls IsPrime with 1, 2, 17, 21 and 7919.

    TaskCheck("hc_prime");

For a nudge:  TaskHint("hc_prime",1);
*/

Bool IsPrime(I64 n)
{
  //Your code goes here. Answering FALSE for everything is right twice out of
  //five, which is not the same as being right.
  return FALSE;
}
