/* Task hc_fib - Fibonacci

  Fib(0) is 0
  Fib(1) is 1
  every later value is the sum of the two before it

The checker calls Fib with 0, 1, 10 and 40. Note the 40: whether your version
answers it at once or seems to hang is the whole point of the exercise.

Run the check when you are ready:

    TaskCheck("hc_fib");

For a nudge:  TaskHint("hc_fib",1);
*/

I64 Fib(I64 n)
{
  //Your code goes here. As written this returns 0 for everything, so one
  //case out of four already passes - that is not the same as being right.
  return 0;
}
